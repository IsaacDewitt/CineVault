# 项目规矩

先说最重要的：不要谈论 Fight Club，开个玩笑，轻松点。但做事要认真，尤其是文件、依赖、下载和版本管理。

## 1. 下载和安装必须先请示

- 不要擅自下载任何文件、模型、数据集、安装包、二进制工具或第三方资源。
- 不要擅自安装任何库、软件、插件、CLI 工具或全局依赖。
- 如果确实需要下载或安装，先说明：为什么需要、会下载什么、来源是否官方、会写入哪些目录、是否有替代方案。
- 等项目主人确认后再执行。复杂下载流程需要写成脚本，并放到合适目录，不能随手丢在项目根目录。
- 下载脚本要可复用、可读、可清理，必要时配套 README 说明用途、参数和输出目录。

## 2. 代码要有主人翁意识

- 新代码必须放在符合项目结构的位置，不要把一坨脚本或临时代码堆在根目录。
- 新增目录时，目录职责要清楚；如果目录用途不直观，补一个简短 README。
- 优先沿用项目已有风格、命名、工具链和模块边界，不要为了炫技引入新架构。
- 改动要聚焦当前任务，不顺手重构无关代码，不制造额外噪音。
- 写完后检查是否留下临时文件、调试日志、废弃脚本、无用输出和重复代码。

## 3. 临时脚本要可追踪、可清理

- 临时分析脚本优先放到 `scripts/` 下，并使用清晰命名，比如 `analyze_xxx.py`、`process_xxx.py`。
- 脚本开头写明用途、输入、输出和是否可以删除。
- 任务完成后主动判断脚本是否还需要保留；不需要的要清理，需要保留的要整理命名和说明。
- 不要把一次性实验代码混进业务目录。

## 4. 遇到设计问题要先调研

- 对不确定的架构、交互、性能或实现方案，不要拍脑袋硬写。
- 优先阅读项目现有实现，再参考成熟开源项目或官方文档。
- 调研结论要落到行动上：推荐方案、取舍理由、影响范围和验证方式。

## 5. 版本管理要谨慎

- 开始大改前先看当前 git 状态，识别哪些改动不是自己做的。
- 不要回滚、覆盖或删除用户已有改动，除非项目主人明确要求。
- 大改尽量分阶段完成，每阶段保持可解释、可测试、可回退。
- 遇到方向明显错了，先说明问题和建议的回退点，再让项目主人决定是否回退。
- 禁止擅自执行破坏性 git 命令，比如 `reset --hard`、强制覆盖、强推等。

## 6. 交付前检查

- 代码改完后，尽量运行与改动范围匹配的检查或测试；不能运行时说明原因。
- 交付说明要讲清楚：改了什么、为什么这么改、怎么验证、还有什么风险。
- 如果新增了依赖、脚本、配置或目录，说明它们的用途和维护方式。
- 保持项目干净：没有多余临时产物，没有无意义格式化，没有无关文件混入。



