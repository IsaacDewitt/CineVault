{
  "identifier": "default",
  "description": "Default capabilities for MovieUI",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "shell:default"
  ]
}
